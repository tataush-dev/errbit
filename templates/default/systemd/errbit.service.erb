[Unit]
Description=erbit server
After=network.target

[Service]
User=errbit
WorkingDirectory=/opt/errbit
ExecStart=bundle exec rails server -b 0.0.0.0 -p 3000
Restart=always
ExecReload=/bin/kill -SIGINT $MAINPID
KillMode=process

[Install]
WantedBy=multi-user.target
